[
  {
    "name": "/price shows error when API key is missing",
    "steps": [
      {
        "send": { "text": "/price" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Gold price data requires an API key. Set GOLD_API_KEY in your environment."
            }
          }
        ]
      }
    ]
  },
  {
    "name": "price menu button shows error when API key is missing",
    "steps": [
      {
        "send": { "callback": "price:show" },
        "expect": [
          { "method": "answerCallbackQuery" },
          {
            "method": "sendMessage",
            "payload": {
              "text": "Gold price data requires an API key. Set GOLD_API_KEY in your environment."
            }
          }
        ]
      }
    ]
  }
]
